# YouTube KOL 工作流 → 飞书同步 · 使用说明与注意事项

> 本文是给人看的操作手册（怎么跑命令、怎么操控）。AI 执行时的技术细节见同目录 `SKILL.md`。
> 面向非技术使用者（怎么看飞书表、筛红人）的业务说明见 `YouTube 网红开发工作流系统/使用说明与注意事项.md`。
> 适用项目：`YouTube 网红开发工作流系统/assets/yt-kol-workflow`
> 目标：把 YouTube 网红搜索结果，同步进**你自己拥有**的飞书多维表格（Base），让你有字段配置 / 建视图的完整权限。

---

## 一、这个 skill 能做什么

1. 用关键词在 YouTube 上搜网红视频 → 按播放量 / 互动率筛选 → 识别频道 → 抓取联系方式。
2. 把结果汇总成 4 张表：**搜索任务表 / 视频数据表 / 网红详情表 / 网红视频表**。
3. 同步进你（用户身份）建的飞书 Base，你就是 owner，可随意改字段、建视图。
4. 从网红详情表派生一张精简的**红人表**（25 列），并保持两表数据一致。

---

## 二、什么时候用

- 「帮我用关键词搜 YouTube 网红，同步到飞书表格」
- 「重跑搜索后，首轮的数据会怎样？」
- 「为什么我在 base 里没权限改字段 / 建视图？」
- 「配置一下飞书同步」
- 「红人表的数据是不是最新 / 怎么更新红人表」

---

## 三、前置条件（第一次准备一次即可）

| 项目 | 说明 |
|---|---|
| YouTube API Key | 写在项目 `.env` 的 `YOUTUBE_API_KEY`，需自行用 Gmail 申请免费配额 |
| 飞书套件已连接 | 即 `lark-cli --as user` 可用（飞书 connector 已连接，浏览器授权过一次） |
| lark-cli 路径 | 真实路径 `/Users/coscod/.workbuddy/binaries/node/cli-connector-packages/bin/lark-cli`（写脚本时用它，别用 write_user_base.py 顶部那个失效常量） |
| 目标 Base | 用**你的身份**建的 Base（你是 owner）。`BASE_TOKEN = <FEISHU_BASE_TOKEN>` |
| 运行环境 | 本机有 Node.js / npx（`lark-cli` 依赖）；Python 用项目 `.venv`（注意未装 python-dotenv，部分脚本要 stub，见 SKILL.md 2.7） |

> ⚠️ **权限根因**：如果用「应用身份（tenant token）」建的 Base，你只是"可阅读"，没法改字段 / 建视图。本方案一律用**你的飞书身份**建 Base，从根上规避。

---

## 四、标准使用流程

### 第一次

```bash
cd YouTube 网红开发工作流系统/assets/yt-kol-workflow

# 1) 配 .env：填好 YOUTUBE_API_KEY（飞书套件已连接）
# 2) 准备关键词文件 keywords.txt（每行一个词）

# 3) 跑批量搜索 → 结果进 output/<时间戳>_batch/
python main.py batch --keywords-file keywords.txt -o output/<时间戳>_batch

# 4) 合并成一个汇总表（4 张表，按 Channel ID 去重）
python main.py merge-output output/<时间戳>_batch -o output/summary_<时间戳>/kol_summary_tables.xlsx

# 5) 同步进你的飞书 Base（日常推荐：增量 upsert，同步完自动更新红人表）
python main.py sync-workbook --workbook output/summary_<时间戳>/kol_summary_tables.xlsx \
    --feishu-app-token <FEISHU_BASE_TOKEN>
```

> 若要做**大版本整表替换**（清空 4 表重灌、保留字段/视图），用 `python write_user_base.py`，
> 但它会丢飞书里逐行手填的值，替换前先确认或备份。
> **首次建 Base + 4 张表**用 `sync_to_user_base.py`（只跑一次）。

### 第二次及以后（关键：不要重搜首轮）

```bash
# 1) 只用【新】关键词跑 batch，output 用【新】目录
python main.py batch --keywords-file keywords_第二轮.txt -o output/<新时间戳>_batch

# 2) merge 时把【旧目录 + 新目录】一起传（位置参数，不是 --batch-dirs）
python main.py merge-output output/<首轮目录> output/<新目录> \
    -o output/summary_累计/kol_summary_tables.xlsx

# 3) 同步（sync-workbook 会按 Channel ID 增量 upsert，不会把首轮清掉；首轮若没进合并表则仍以主库现有为准）
python main.py sync-workbook --workbook output/summary_累计/kol_summary_tables.xlsx \
    --feishu-app-token <FEISHU_BASE_TOKEN>
```

**为什么这样？** 见第六节的「配额」与「数据命运」。一句话：关键词只搜一次，跨轮累计靠 `merge-output` 合并目录，不要再把首轮关键词塞回 `batch`。

### 存量红人补算（无需重跑搜索）

```bash
# 补算亚马逊推广经验 + 商务邮箱（走 YouTube API 抓视频描述，需翻墙代理，必须后台跑）
./.venv/bin/python refresh_promo_lark.py            # 全量（跳过已处理，断点续传）

# 补算评分 6 列（只读飞书，不耗 YouTube 配额）
./.venv/bin/python score_influencers.py --dry-run   # 先预览
./.venv/bin/python score_influencers.py             # 写回

# 手动同步红人表（若没有走 sync-workbook）
./.venv/bin/python sync_hongren_table.py            # 增量 upsert，不删表
./.venv/bin/python sync_hongren_table.py --dedupe   # 额外清理重复 Channel ID

# 刷新本地缓存（写飞书后必跑）
./.venv/bin/python export_local_cache.py influencers
```

---

## 五、你能操控什么

### 搜索阶段（跑搜索前给定）
| 参数 | 给法 | 说明 |
|---|---|---|
| YouTube API Key | `.env` | 必需 |
| 关键词 | `--keywords-file` 文件 / 交互输入 | 每行一个 |
| 每词最大结果数 | 文件内 `词,数量` 或 `--max-results`（默认 100） | 影响配额 |
| 地区 / 语言 | `--region`(默认 US) / `--lang`(默认 en) | |
| 过滤条件 | `--min-views`(默认1万) / `--min-engagement`(默认3.0) / `--min-subscribers` / `--filter-mode` | 筛低质频道 |

### 飞书同步阶段
| 可操控项 | 说明 |
|---|---|
| 目标 Base | 你的身份建的 Base，token = `<FEISHU_BASE_TOKEN>`（写在脚本 BASE_TOKEN） |
| 表名映射 | `write_user_base.py` 的 `SHEETS` / `TABLE_IDS` |
| 字段类型推断 | `infer_type()`：URL→url、日期→date、数字→number、指定列→select、其余→text |
| 单选列白名单 | `SELECT_COLS`：断更评估 / 国家地区 / 邮箱状态 / 邮箱来源 / 亚马逊推广经验 / 开发优先级 / 开发状态 / 来源关键词 |

### 你在飞书界面里能做的（正因 Base 是你建的）
- 改字段配置（加 / 删字段、改类型、给单选加选项、调顺序）
- 建任意视图（筛选 / 分组 / 排序 / 看板）

---

## 六、重要注意事项（必看）

1. **权限**：Base 必须用你的身份建（你是 owner），否则只能看不能改。应用身份建的只读 Base 不要用于日常维护。

2. **配额 —— 别把首轮关键词塞回 batch**：
   `batch` 对每个关键词**无条件重新搜索** YouTube（`search.list`，约 100 配额/词），代码里**没有"这个词搜过就跳过"的缓存**。
   把首轮 + 二轮关键词放一起跑，首轮数据虽靠去重不丢，但首轮关键词会被**白搜一遍、浪费配额**。
   ✅ 正确做法：关键词只搜一次；跨轮累计靠 `merge-output` 把新旧目录一起传。

3. **手动标注会不会丢**：
   - **日常增量同步（sync-workbook / sync_hongren_table）会保护人工列**：备注 / 开发状态 / 联系邮箱 /
     开发负责人 / 开发优先级 —— 主表为空或红人表已有值就不覆盖，你的跟进记录不会丢。
   - **只有 `write_user_base.py`（清空重灌）会清掉手填值**。大版本替换前先确认或备份。
   - 字段配置和视图**始终保留**（脚本只动记录，不动结构）。

4. **`write_user_base.py` 的路径是写死的**：
   第 5 行 `LARK` 指向**已失效**的 node 22.22.2 目录 —— 直接跑会 token_missing。写新脚本时用
   `wb.LARK = "/Users/coscod/.workbuddy/binaries/node/cli-connector-packages/bin/lark-cli"` 覆盖。
   `BASE_TOKEN` 第 8 行指向 VyH0。每次重新 `merge-output` 后记得改 `XLSX` 指向新汇总表。

5. **lark-cli 的 `--json` 不支持绝对路径**：必须 `@./相对名.json`（脚本同目录）。内联字符串超命令行长度上限（1450 行会炸），务必写文件 + `@相对路径`。

6. **清空是最终一致**：删记录后立刻 list 可能还读到旧数据。清表要循环「列举 → 删除」直到连续两次读到 0，否则会叠加重复行。

7. **`main.py sync-workbook` 现在可以用了**（走 lark-cli 用户身份 + 增量 upsert + 保护人工列），
   同步完主表会**自动同步红人表**（`--skip-hongren` 可跳过）。旧文档说"别用原生 sync-workbook"
   已过时——那是早期应用身份版本的问题。

8. **合并按 Channel ID 去重**：同一网红跨多个关键词出现，合并后只保留一条，不会重复。

9. **红人表是派生表，但不再"删表重建"**：用 `sync_hongren_table.py` 增量同步（不删表，
   table_id 永久固定 `tblPKFENcpk8xnZH`）。旧 `build_hongren_table.py`（删表重建、换 ID）已弃用。

10. **写飞书后必跑 `export_local_cache.py influencers` 刷新本地缓存**，否则分析脚本读到旧数据。
    （该脚本顶层 import dotenv 但 venv 未装 → 需 stub 或先 pip install python-dotenv。）

---

## 七、常见问题

**Q：为什么我没权限改字段 / 建视图？**
A：Base 是用「应用身份」建的，你只有阅读权。解决：用飞书套件（你的身份）重建一个 Base，你就是 owner，全权限。

**Q：第二次搜关键词，第一次的数据会丢吗？**
A：取决于 `merge-output` 传了哪些目录——
- 只传二轮目录 + 走 `write_user_base.py` 清空重灌 → 首轮从 Base 消失。
- 旧 + 新目录都传 → 首轮 + 二轮合并去重，全保留。
- 走 `sync-workbook`（增量 upsert）→ 只增改不删，主库已有记录保留。
- 首轮文件仍在磁盘旧目录，只是没被合并就不在表里。

**Q：重搜首轮关键词会不会？**
A：**会**，而且白费配额（见注意事项 2）。想累计数据请用「新关键词单独跑 + merge 合并目录」。

**Q：我在飞书里改了字段、加了视图，下次同步还在吗？**
A：在。字段配置和视图始终保留；逐行手填的**人工列**在增量同步下也被保护（备注 / 开发状态 /
联系邮箱等已有值不覆盖），只有 `write_user_base.py` 清空重灌会覆盖。

**Q：红人表怎么更新？**
A：日常：`sync-workbook` 同步完主表会自动带；手动：`python sync_hongren_table.py`（增量、不删表、幂等可重跑）。

---

## 八、命令速查

```bash
# 搜（首轮 / 新词，各自独立目录）
python main.py batch --keywords-file <词文件> -o output/<时间戳>_batch

# 合并（位置参数传目录，可多个；-o 指定输出 xlsx）
python main.py merge-output output/<目录A> output/<目录B> -o output/summary/kol_summary_tables.xlsx

# 同步（日常推荐：增量 upsert + 自动带红人表）
python main.py sync-workbook --workbook <汇总.xlsx> --feishu-app-token <FEISHU_BASE_TOKEN>

# 大版本整表替换（清空重灌；先确认人工标注已备份）
python write_user_base.py

# 补算 promo + 邮箱（后台跑；断点续传）
./.venv/bin/python refresh_promo_lark.py

# 补算评分（预览 / 写回）
./.venv/bin/python score_influencers.py --dry-run
./.venv/bin/python score_influencers.py

# 红人表增量同步（含去重）
./.venv/bin/python sync_hongren_table.py
./.venv/bin/python sync_hongren_table.py --dedupe

# 刷新本地缓存
./.venv/bin/python export_local_cache.py influencers
```
